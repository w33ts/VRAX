# Changelog

All notable changes to this project are documented in this file.

## [0.0.2] - 2026-02-27

### Changed
- Reorganized UI files into feature-focused folders under `Views/Windows/`.
- Updated application namespaces from `ReconSuite` to `VRAX` for consistency with the repository and project identity.
- Updated app startup URI to the new `MainWindow.xaml` location.

### Build and Release
- Incremented semantic version in `VRAX.csproj` to `0.0.2` (`AssemblyVersion` and `FileVersion` set to `0.0.2.0`).
